{
  "name": "xpgain-firestore-rules-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Security-rules tests for the XP_Gain Firestore database.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-xpgain \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.23.0"
  },
  "engines": {
    "node": ">=18"
  }
}
